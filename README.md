# Škola

Laravel and Vue.js application.

## Installation

Clone and install dependencies.

Setup database connection in .env file. Use .env.example.


Run:

```bash
php artisan migrate
php artisan serve
npm run dev
```

Go to: http://127.0.0.1:8000/